query Input {
    cart {
        attribute(key: "_radiusDiscounts") {
            value
        }
        lines {
            id
            quantity
            attribute(key: "_bundle_id") {
                value
            }
            cost {
                subtotalAmount {
                    amount
                }
            }
            merchandise {
                __typename
                ... on ProductVariant {
                    id
                    product {
                        id
                    }
                }
            }
        }
    }
    discount {
        discountClasses
        metafield(namespace: "$app", key: "active_bundles") {
            value
        }
    }
}
