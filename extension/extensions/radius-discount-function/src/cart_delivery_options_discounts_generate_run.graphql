query Input {
    cart {
        attribute(key: "_radiusDiscounts") {
            value
        }
        deliveryGroups {
            id
        }
    }
    discount {
        discountClasses
        metafield(namespace: "radius_bundles", key: "active_bundles") {
            value
        }
    }
}
