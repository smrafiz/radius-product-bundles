query Input {
    cart {
        attribute(key: "_radiusDiscounts") {
            value
        }
        deliveryGroups {
            id
        }
    }
    discount {
        discountClasses
        metafield(namespace: "$app", key: "active_bundles") {
            value
        }
    }
}
