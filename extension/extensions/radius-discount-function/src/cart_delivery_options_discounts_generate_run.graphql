query Input {
    cart {
        attribute(key: "_radiusDiscounts") {
            value
        }
        deliveryGroups {
            id
        }
    }
    discount {
        discountClasses
    }
}