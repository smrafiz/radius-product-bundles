query Input {
    cart {
        attribute(key: "_bundleDiscounts") {
            value
        }
        deliveryGroups {
            id
        }
    }
    discount {
        discountClasses
    }
}