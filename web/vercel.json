{
    "framework": "nextjs",
    "buildCommand": "bunx prisma db push --accept-data-loss && bun prepare && bun graphql-codegen && next build",
    "crons": [
        {
            "path": "/api/cron/bundle-scheduler",
            "schedule": "0 0 * * *"
        }
    ]
}
