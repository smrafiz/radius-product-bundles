{
    "framework": "nextjs",
    "buildCommand": "bun prisma:push && bun prepare && bun graphql-codegen && next build",
    "crons": [
        {
            "path": "/api/cron/bundle-scheduler",
            "schedule": "0 0 * * *"
        }
    ]
}
