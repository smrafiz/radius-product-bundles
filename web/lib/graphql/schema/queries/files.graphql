query filesCheckOrphaned($fileQuery: String!) {
    files(first: 1, query: $fileQuery) {
        edges {
            node {
                ... on MediaImage {
                    id
                }
            }
        }
    }
}
