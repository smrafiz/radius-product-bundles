# Database
DATABASE_URL="postgresql"
DIRECT_DATABASE_URL="${DATABASE_URL}"

# Cron job authentication (Run below command on terminal to get the key)
CRON_SECRET="node -e "console.log(crypto.randomUUID())"

# Token encryption key (Run below command on terminal to get the key)
ENCRYPTION_KEY="node -e "console.log(require('crypto').randomBytes(32).toString('hex'))""
# Annual billing test override (dev only — dev stores never charge real money)
# NEXT_PUBLIC_FORCE_ANNUAL_TEST=true
